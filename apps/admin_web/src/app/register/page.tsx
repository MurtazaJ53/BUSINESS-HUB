import { AuthRegister } from "@/components/auth-register";

export const metadata = {
  title: "Register Store | Business Hub Cloud",
  description: "Create a new store on Business Hub Cloud",
};

export default function RegisterPage() {
  return <AuthRegister />;
}
