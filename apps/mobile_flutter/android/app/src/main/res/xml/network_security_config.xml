<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP stays OFF everywhere except loopback, which is where the
  `adb reverse tcp:8000 tcp:8000` dev tunnel lands. Real backends must be
  HTTPS - do not widen this to a LAN range or a domain.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- Android emulator's alias for the host machine. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
